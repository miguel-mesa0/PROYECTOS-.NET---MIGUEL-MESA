@{
    ViewData["Title"] = "Inicio";
}

<div class="text-center mt-5">
    <h1 class="display-4 mb-4">Sistema de Ventas</h1>
    <p class="lead mb-5">Selecciona una opción para administrar los datos:</p>

    <div class="d-flex justify-content-center gap-4">
        <a href="@Url.Action("Index", "Clientes")" class="btn btn-primary btn-lg px-5">
             Clientes
        </a>
        <a href="@Url.Action("Index", "Productoes")" class="btn btn-success btn-lg px-5">
             Productos
        </a>
        <a href="@Url.Action("Index", "Ventas")" class="btn btn-warning btn-lg px-5">
             Ventas
        </a>
    </div>
</div>
