@model Inventario.Models.Inventario.Proveedore

@{
    ViewData["Title"] = "Detalles de Proveedor";
}

<div class="max-w-xl mx-auto bg-white rounded-xl shadow-md p-6">
    <h1 class="text-xl font-bold text-slate-800 mb-4">Detalles de Proveedor</h1>

    <dl class="space-y-3 text-sm">
        <div>
            <dt class="font-semibold text-slate-600">Nombre</dt>
            <dd class="text-slate-800">@Model.Nombre</dd>
        </div>
        <div>
            <dt class="font-semibold text-slate-600">Documento</dt>
            <dd class="text-slate-800">@Model.NIT</dd>
        </div>
        <div>
            <dt class="font-semibold text-slate-600">Telefono</dt>
            <dd class="text-slate-800">@Model.Telefono</dd>
        </div>
        <div>
            <dt class="font-semibold text-slate-600">Email</dt>
            <dd class="text-slate-800">@Model.Email</dd>
        </div>
        <div>
            <dt class="font-semibold text-slate-600">Dirección</dt>
            <dd class="text-slate-800">@Model.Direccion</dd>
        </div>
    </dl>

    <div class="flex justify-between items-center mt-6">
        <a asp-action="Index" class="text-xs text-slate-500 hover:text-slate-700">
            ← Volver a la lista
        </a>

        <a asp-action="Edit" asp-route-id="@Model.Id"
           class="px-4 py-2 bg-amber-500 hover:bg-amber-600 text-white text-xs font-semibold rounded-lg">
            Editar
        </a>
    </div>
</div>
